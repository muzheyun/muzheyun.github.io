<template>
  <div v-if="open" class="mainDiv">
    <a-row>
      <div class="mainTable" id="capture">
        <div class="title-box">
          <a-row class="title">App Privacy Nutrition</a-row>
          <h3>Generated By U-PET</h3>
        </div>
        <div v-for="model in props.models">
          <div v-for="oneLine in model">
            <SubTitle v-if="oneLine.type === 'subTitle'" :title="oneLine.value"></SubTitle>
            <OneLine v-else :oneLine="oneLine"></OneLine>
          </div>
        </div>
      </div>
    </a-row>
    <a-row>
      <a-button @click="close">Close</a-button>
    </a-row>
  </div>
</template>

<script setup>
import OneLine from './OneLine.vue'
import SubTitle from './SubTitle.vue'
const emit = defineEmits(['close'])
let qa1 = {
  question: "Man",
  ans: "Yes"
}
let props = defineProps({
  open: {
    type: Boolean,
    required: true
  },
  models: {
    type: Array,
    required: true
  }
});
function close() {
  console.log('emit close!');
  emit('close');
  console.log('emit close, result: ' + props.open);
}

</script>

<style scoped>
.mainDiv {
  color: black;
  position: fixed;
  z-index: 9998; /* 使用一个较高的z-index值确保它在顶部 */
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 400px;
  /*height: 900px;*/
  overflow: scroll;
  max-height: 800px; /* 设置最大高度，超过此高度将显示滚动条 */
  background-color: #ffffff; /* 可选：设置背景颜色，使内容更加醒目 */
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.5); /* 添加阴影效果 */
}
.mainTable {
  /*position: fixed;*/
  z-index: 9999; /* 使用一个较高的z-index值确保它在顶部 */
  width: 100%;
  height: 100%;
  padding: 10px;
  border: 1px solid #000000; /* 深蓝色边框，1px宽 */
}
.title-box {
  padding: 10px;
  border: 3px solid #181818;
  border-bottom: 6px solid #181818;
}
.title {
  font-size: 24px;
  font-weight: 700;
  margin-bottom: 10px;
}
</style>