<template>
  <div v-if="open" class="mainDiv">
    <a-row>
      <div class="mainTable" id="capture">
        <div class="title">
          <h1>App Privacy Nutrition</h1>
          <h3>Generated By U-PET</h3>
        </div>
      </div>
    </a-row>
    <a-row>
      <a-button @click="close">Close</a-button>
    </a-row>
  </div>
</template>

<script setup>
const emit = defineEmits(['close'])
let props = defineProps({
  formState: {
    type: Object,
    required: true
  },
  open: {
    type: Boolean,
    required: true
  }
});
function close() {
  console.log('emit close!');
  emit('close');
  console.log('emit close, result: ' + props.open);
}

</script>

<style scoped>
.mainDiv {
  color: black;
  position: fixed;
  z-index: 9998; /* 使用一个较高的z-index值确保它在顶部 */
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 500px;
  height: 600px;
  background-color: #ffffff; /* 可选：设置背景颜色，使内容更加醒目 */
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.5); /* 添加阴影效果 */
}
.mainTable {
  /*position: fixed;*/
  z-index: 9999; /* 使用一个较高的z-index值确保它在顶部 */
  width: 100%;
  height: 100%;
  padding: 10px;
  border: 1px solid red; /* 深蓝色边框，1px宽 */
}
.title {
  padding: 10px;
  border: 3px solid #181818;
  border-bottom: 6px solid #181818;
}
</style>